pipeline {
    agent any
    triggers {
        pollSCM('H/5 * * * *')
    }
    stages {
        stage('Hello world') {
            steps {
                echo "Running some test..."
            }
        }
    }
}
